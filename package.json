{
  "scripts": {
    "build": "bundle install; bundle exec jekyll build; mv _site public"
  },
  "devDependencies": {
    "prettier": "^1.14.3"
  },
  "prettier": {
    "printWidth": 120,
    "proseWrap": "always"
  }
}
