{
  "scripts": {
    "build": "bundle install; bundle exec jekyll build; mv _site public; ls -R public"
  },
  "devDependencies": {
    "prettier": "^1.14.3"
  },
  "prettier": {
    "printWidth": 120,
    "proseWrap": "always"
  }
}
